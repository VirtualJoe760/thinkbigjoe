# Define the team — small intake

**Name + goal come from the command:** `/create-team <name> <goal>`. A few words per line; research
fills the rest. After this I write `TEAM.md`, then we build the members one at a time.

---

**Team:** `<name>` · **Goal:** `<goal>`  *(from the command)*

- **Manager** — who coordinates the team and talks to Venus? (a dedicated manager, or which member):
- **Roster** — the members + a one-line role each:
- **Pipeline** — who hands to whom · who reviews · who tests · where YOU approve:
- **Create all members now?** (yes → I run `/create-agent` for each in order; no → just set up the team):
- **Shared context** — anything the whole team must know (the codebases, conventions, the mission):

---

> Venus sits above the manager; the human gate (you) is the last checkpoint at the riskiest step.
> Keep the roster small (2–4 works best) — every extra agent adds failure surface.
