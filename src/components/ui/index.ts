// Barrel for the shared UI primitives. Import from "@/components/ui" rather than the individual
// files. Each primitive follows the same recipe: lookup tables + a `*Class()` helper (className-only
// migration lever) + the component.
export * from "./button";
export * from "./card";
export * from "./field";
export * from "./badge";
